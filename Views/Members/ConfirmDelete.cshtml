@* Views/Members/ConfirmDelete.cshtml *@
@model HotBubbleCanteen.Models.Member

<div class="container mt-4">
    <h3>Are you sure you want to delete this membership?</h3>
    <p><strong>Membership Number:</strong> @Model.Id</p>
    <p><strong>@Model.FirstName @Model.LastName</strong> — @Model.PhoneNumber</p>

    <form asp-action="DeleteConfirmed" method="post">
        <input type="hidden" name="id" value="@Model.Id" />
        <button type="submit" class="btn btn-danger">Yes, delete</button>
        <a asp-action="Search" class="btn btn-secondary ms-2">Cancel</a>
    </form>
</div>